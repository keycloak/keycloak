import CommonPage from "../../../../../../CommonPage";

export enum SetupTabFilterItem {
  Name = "Name",
  AssignedType = "Assigned type",
}

export enum SetupTabAssignedTypeItem {
  AllTypes = "All types",
  Default = "Default",
  Optional = "Optional",
}

export default class SetupTab extends CommonPage {}
