/**
 * Represents a configuration of credential offer action
 * */
export default interface CredentialOfferActionConfigRepresentation {
  credential_configuration_id?: string;
  client_id?: string;
  pre_authorized?: boolean;
}
