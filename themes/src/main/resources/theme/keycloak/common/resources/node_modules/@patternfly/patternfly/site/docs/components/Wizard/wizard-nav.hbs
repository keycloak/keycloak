<nav class="pf-c-wizard__nav{{#if wizard--IsExpanded}} pf-m-expanded{{/if}}{{#if wizard-nav--modifier}} {{wizard-nav--modifier}}{{/if}}"
  aria-label="Steps"
  {{#if wizard-nav--attribute}}
    {{{wizard-nav--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</nav>
