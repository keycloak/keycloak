<nav class="pf-c-breadcrumb{{#if breadcrumb--modifier}} {{breadcrumb--modifier}}{{/if}}"
  aria-label="breadcrumb"
  {{#if breadcrumb--attribute}}
    {{{breadcrumb--attribute}}}
  {{/if}}>
  {{> @partial-block}}
</nav>