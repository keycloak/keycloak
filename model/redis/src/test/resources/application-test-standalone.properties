# Standalone Test Environment Configuration
# This file contains environment-specific properties for local development testing.
# Common properties are loaded from application-test.properties

# Redis Connection Configuration (Standalone/Local)
redis.host=localhost
redis.port=16379
redis.cluster.enabled=false

# Keycloak Configuration (Local)
keycloak.base.url=http://localhost:18080
