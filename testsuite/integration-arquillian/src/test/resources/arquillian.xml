<arquillian xmlns="http://jboss.org/schema/arquillian"
            xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
            xsi:schemaLocation="http://jboss.org/schema/arquillian
        http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <defaultProtocol type="Servlet 3.0" />
	
    <container qualifier="keycloak-remote" />

    <group qualifier="default-group" default="true" >
        <container qualifier="keycloak-managed" mode="manual" default="true">
            <configuration>
                <property name="jbossHome">${keycloak.home}</property>
                <property name="javaVmArguments">-Djboss.socket.binding.port-offset=${keycloak.port.offset} -Dapp.server.base.url=http://localhost:${wildfly.http.port} -Dmy.host.name=localhost</property>
                <property name="managementPort">${keycloak.management.port}</property>
                <property name="adapterImplClass">org.jboss.as.arquillian.container.managed.ManagedDeployableContainer</property>
            </configuration>
        </container>
        <container qualifier="wildfly-adapter-managed" mode="manual" >
            <configuration>
                <property name="jbossHome">${wildfly.home}</property>
                <property name="javaVmArguments">-Djboss.socket.binding.port-offset=${wildfly.port.offset} -Dapp.server.base.url=http://localhost:${wildfly.http.port} -Dmy.host.name=localhost</property>
                <property name="managementPort">${wildfly.management.port}</property>
                <property name="adapterImplClass">org.jboss.as.arquillian.container.managed.ManagedDeployableContainer</property>
            </configuration>
        </container>
        <container qualifier="embedded-undertow-adapter-managed" mode="manual" >
            <configuration>
                <property name="bindAddress">localhost</property>
		<property name="bindHttpPort">${undertow.http.port}</property>
                <property name="adapterImplClass">org.keycloak.testsuite.arquillian.undertow.EmbeddedUndertowContainer</property>
            </configuration>
        </container>
    </group>
    
    <extension qualifier="webdriver">
        <property name="browser">${browser}</property>
    </extension>
    
    <engine>
        <property name="deploymentExportPath">target/deployments</property>
    </engine>
    
</arquillian>
